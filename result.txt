db.studentsDB.find({
  scores: {
    $elemMatch: {
      score: {
        $gt: 87,
        $lt: 93
      } 
    }
  }
}).pretty();

db.studentsDB.aggregate([{
  $unwind: "$scores"
  }, 
  {
    $match: {
      'scores.type' : 'exam',
      'scores.score': {
        $gt: 90
      } 
    }
  }
]).pretty();

db.studentsDB.update(
  {
    name: "Dusti Lemmond" 
  },
  {
    $set: {accepted: true}
  },
  {
    multi: true
  }
);